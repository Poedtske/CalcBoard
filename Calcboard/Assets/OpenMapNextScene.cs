using UnityEngine;

public class OpenMapNextScene : MonoBehaviour
{

    /// <summary>
    /// Start is called before the first frame update
    /// deze is de test om te zien of object met nodige info naar de volgende scene gaat
    /// na he het testen blijkt te werken en moet gwn nog aan de juiste scene gekoppeld worden
    /// </summary>
    //void Start()
    //{
    //    // Access the singleton instance that stores your ElektroMap data
    //    if (CreateMap.Instance != null && CreateMap.Instance.elektroMapData != null)
    //    {
    //        // Successfully retrieved the data from the singleton
    //        Debug.Log("Map Name: " + CreateMap.Instance.elektroMapData.MapName);
    //        Debug.Log("Languages: " + string.Join(", ", CreateMap.Instance.elektroMapData.Languages));
    //    }
    //    else
    //    {
    //        Debug.LogError("ElektroMap data not found!");
    //    }
    //}
}
